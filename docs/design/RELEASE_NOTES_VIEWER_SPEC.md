# Release Notes Viewer

Route `/release-notes` renders the redacted text returned by the official GitHub release API. It identifies version, publication time, prerelease state, declared breaking-change bullets, and the canonical release URL. Remote HTML is never injected.

Empty/error states retain the current installed version and a retry path.

© CODINFY PLATFORMS SASU · codinfy.com
